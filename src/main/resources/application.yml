spring:
  datasource:
    url: jdbc:mysql://nuc/homepage?zeroDateTimeBehavior=convertToNull
    username: test
    password: test

    testWhileIdle: true
    validationQuery: SELECT 1

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
